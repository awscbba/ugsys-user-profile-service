#!/usr/bin/env bash
set -euo pipefail

echo ""
echo "🔍 Running pre-push checks..."
echo ""

echo "── Unit tests ──"
if uv run pytest tests/unit/ -q --tb=short; then
  echo "  ✓ tests passed"
else
  echo "  ✗ tests failed"
  exit 1
fi

echo ""
echo "✓ Pre-push checks passed."
echo ""
